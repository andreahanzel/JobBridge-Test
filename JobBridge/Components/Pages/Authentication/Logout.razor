@page "/authentication/logout"
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.AspNetCore.Components.Web
@using JobBridge.Services
@using Microsoft.JSInterop
@inject NavigationManager Navigation
@inject AuthService AuthService
@inject SignInManager<JobBridge.Data.User> SignInManager

<link href="css/authentication.css" rel="stylesheet" /> <!-- This is the authentication CSS -->

<PageTitle>Logout - JobBridge</PageTitle>

<!-- This part is for the logout confirmation -->
<AuthorizeView>
    <Authorized>
        <div class="container mt-5 animate-fadeInUp auth-container">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <h2 class="text-center mb-4 text-primary">Log Out</h2>
                    <div class="card p-4 shadow-sm text-center auth-card">
                        <p>Are you sure you want to log out?</p>
                        <button class="btn btn-primary animate-pulse" @onclick="HandleLogout">Log Out</button>
                        <div class="text-center mt-3">
                            <a href="/" class="text-muted">Return to Home</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </Authorized>
    <!-- This part is the login confirmation -->
    <NotAuthorized>
        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="card p-4 shadow-sm text-center">
                        <p>You are not logged in.</p>
                        <a href="/authentication" class="btn btn-primary">Go to Login</a>
                    </div>
                </div>
            </div>
        </div>
    </NotAuthorized>
</AuthorizeView>

@code {

    // This is the logout handler
    private async Task HandleLogout()
    {
        await SignInManager.SignOutAsync();
        Navigation.NavigateTo("/", true);
    }
}